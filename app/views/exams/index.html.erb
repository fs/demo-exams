	<h1>Exam choosing list<span><%= link_to_if(current_user.admin?, 'Create new exam', new_exam_path) %></span></h1>
<% @exams.each do |exam| %>
	<%= render(exam) %>
  
  <%= link_to 'Show', exam %>
  <%= link_to_if(current_user.admin?, 'Update', edit_exam_path(exam.id)) %>
<% end %>

