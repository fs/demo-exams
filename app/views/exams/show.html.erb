<%= render(@exam) %>

<% form_for @user_exam do |form| %>
  <%= form.hidden_field :exam_id %>
  <%= form.submit 'Start exam', :disable_with => 'Please wait...' %>
<% end %>

or

<%= link_to 'Cancel', :back %>
